describe('useMarketingSessionParams', () => {
  it.todo(
    'should return empty utmParams and utmiParams if no marketingSession parameters are found'
  )
  it.todo('should return empty objects if sessionPromise is nullish')
  it.todo(
    'should return empty objects if no public fields are found within session'
  )
  it.todo('should return empty utmParams if there are no utm fields')
  it.todo('should return empty utmiParams if there are no utmi fields')
  it.todo('should correctly return all utmParams')
  it.todo('should correctly return all utmiParams')
})
